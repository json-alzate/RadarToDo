import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jheison.radartodo',
  appName: 'RadarToDo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
